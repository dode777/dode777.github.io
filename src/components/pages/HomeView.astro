---
import Page from '../../layouts/Page.astro';
import Container from '../core/Container.astro';
import Section from '../sections/Section.astro';
import HomeHero from '../sections/HomeHero.astro';
import ServiceCard from '../cards/ServiceCard.astro';
import ContactSection from '../sections/ContactSection.astro';
import { services } from '../../config/services.js';
import { getLangFromUrl, useTranslations } from '../../i18n/ui.js';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Page>
	<HomeHero />

	<Container>
		<section class="services" id="services" aria-label={t('site.title')}>
			<div class="services__grid">
				{services.map((service) => <ServiceCard service={service} />)}
			</div>
		</section>
	</Container>

	<Section title={t('contact.title')} id="contact" surface narrow={false}>
		<ContactSection />
	</Section>
</Page>

<style>
	.services {
		margin: var(--section-margin) auto;
		scroll-margin-top: 5.5rem;
	}
	.services__grid {
		display: grid;
		/* auto-fit 은 빈 트랙을 접기 때문에, 항목이 하나면 카드가 한 줄을 채우고
		   둘 이상이면 두 칸으로 나뉩니다. */
		grid-template-columns: repeat(auto-fit, minmax(min(100%, 28rem), 1fr));
		gap: var(--theme-grid-gap);
	}
</style>
